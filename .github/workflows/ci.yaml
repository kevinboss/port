name: Build and test

on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v6.0.0
        with:
          dotnet-version: 10.0.x
      - name: Install dependencies
        run: dotnet restore src/port/port.csproj
      - name: Build
        run: dotnet build src/port/port.csproj --configuration Release
      - name: Test
        run: dotnet test src/port/port.csproj --no-restore --verbosity normal
